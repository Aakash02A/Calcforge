package com.calcforge.engine;

/** The five canonical stages of a CalcForge calculation trail: Input, Assumptions, Formula, Computation, Result. */
public enum TrailStage {
    INPUT,
    ASSUMPTIONS,
    FORMULA,
    COMPUTATION,
    RESULT
}
