package com.calcforge.engine;

/** Lexical categories produced by {@link Lexer}. */
public enum TokenType {
    NUMBER,
    IDENTIFIER,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    PERCENT,
    CARET,
    BANG,
    LPAREN,
    RPAREN,
    COMMA,
    EOF
}
